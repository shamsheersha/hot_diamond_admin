import 'package:flutter/material.dart';

class CustomColors {
  static const Color primaryColor = Color(0xFF000000); // Black (Used in app buttons, text)
  static const Color secondaryColor = Color(0xFF4CAF50); // Green (Used in success notifications or buttons)
  static const Color accentColor = Colors.red; // Orange (Used for highlighting important actions)

  static const Color backgroundColor = Colors.white; // Light Grey (Used as background color for screens)
  static const Color textColor = Color(0xFF212121); // Dark Grey (Default text color)
  static const Color buttonTextColor = Colors.white;
  static const Color redButtonTextColor = Colors.red; // White (Used for button text)

  static const Color errorColor = Color(0xFFFF0000); // Red (Used for error notifications or icons)
  static const Color successColor = Color(0xFF4CAF50); // Green (Used for success notifications)

  // Custom colors for other elements
  static const Color profileAvatarBackground = Color(0xFFE0E0E0); // Light grey for profile avatar background
  static const Color dividerColor = Color(0xFFBDBDBD); // Grey for dividers
}
